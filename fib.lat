int fib(int n) {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

int main(){
    if (letsgo() && true)
        printInt(fib(10));
    else 
        printString("hihi");
    return 0;
}

boolean letsgo() {
    return true;
}